defmodule EvmscanApi.Graphql.Resolver.Block do
  def block(_, _, _) do
    {:ok, nil}
  end

  def blocks(_, _, _) do
    {:ok, nil}
  end

  def transactions(_, _, _) do
    {:ok, nil}
  end

  def withdrawals(_, _, _) do
    {:ok, nil}
  end
end
